specVersion: "v1beta"
serviceId: "multiples-main"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "multiples-main-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
